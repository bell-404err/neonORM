// Type-safe model creation helper for defining database schemas
// Wraps schema objects to provide TypeScript inference and validation
// Use this instead of plain objects to get autocomplete for field configurations

export { createModel } from '@utils/index.js';
